import React from "react";

function Row({title}) {
    const [movies, setMovie]
    
    return (
        <div>
            <h2>{title}</h2>

            {/* container -> posters */}
        </div>
    )
}

export default Row